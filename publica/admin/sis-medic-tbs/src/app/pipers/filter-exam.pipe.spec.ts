import { FilterExamPipe } from './filter-exam.pipe';

describe('FilterExamPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExamPipe();
    expect(pipe).toBeTruthy();
  });
});
